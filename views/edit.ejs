<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>edit</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
  </head>
  <body>
    <header>
      <a href="/" class="header-logo">LIST APP</a>
    </header>
    <div class="container">
      <div class="container-header">
        <h1>edit court</h1>
        <a href="/index" class="cancel-button">cancel</a>
      </div>
      <div class="item-form-wrapper">
        <p class="form-label">court</p>
        <form action="/update/<%= teams[0].id %>" method="post">
          <select class="CourtNum" name="itemName">
            <% courts.forEach((court) => { %>
              <option hidden>選択</option>
              <option><%= court.court %></option>
            <% }) %> 
          </select>
          <select class="CourtNum" name="itemNum">
            <% courts.forEach((court) => { %>
              <option hidden>選択</option>
              <option><%= court.number %></option>
            <% }) %> 
          </select>
          <input type="submit" value="更新する" >
        </form>
      </div>
    </div>
  </body>
</html>